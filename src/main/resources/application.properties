spring.datasource.url=${DB_URL:jdbc:postgresql://db.kdznuteggmfxmxprbwva.supabase.co:5432/postgres}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:work123}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# HS256 signing key, must be at least 32 bytes. Override in production.
app.jwt.secret=${JWT_SECRET:classtracker-dev-secret-key-override-me-in-production}
# 7 days
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:604800000}
